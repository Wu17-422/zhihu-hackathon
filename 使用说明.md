# 犀利评手 · 知乎评论逻辑分析

> 给知乎评论区装一个「AI 裁判」：一眼看清大家在吵什么、谁在逻辑上站得住、哪里有漏洞。
> 支持：逻辑分析、立场比例、相关帖子推荐、思维导图、写评论实时润色。

---

## 一、项目是什么

一个浏览器扩展（Edge / Chrome 通用）+ 本地后端。你在逛知乎时，点一下按钮，AI 就帮你：

| 功能 | 说明 |
|---|---|
| ⚔️ **逻辑分析** | 对评论/回答逐条分析：立场（赞成/反对/中立）+ 想表达什么 + 推理链 + 逻辑漏洞 |
| 📊 **立场比例条** | 彩色横条一眼看清评论区站哪边 |
| 🔗 **相关帖子推荐** | 用知乎真实搜索，推荐相关帖子（可点击跳转） |
| 🧠 **思维导图** | 把帖子/回答正文转成树状导图 |
| ✍️ **写评论实时润色** | 边打字边自动挑逻辑漏洞 + 给改好版本 |
| 📚 **黑客松故事/知识** | 知乎黑客松专属内容接口（免费） |

---

## 二、目录结构

```
zhihu-hackathon/
├── backend/                # Python 后端（FastAPI）
│   ├── main.py             #   接口入口（含启动横幅、/api/health）
│   ├── zhida.py            #   核心逻辑（直答/搜索/缓存/流式解析）
│   ├── prompt.py           #   AI 提示词（产品灵魂）
│   ├── mock.py             #   假数据兜底
│   ├── demo.html           #   公网 Demo：复刻知乎的单页应用（首页/热榜/发现/会员/6 个问题）
│   ├── start.bat           #   一键启动（双击，Windows）
│   ├── start.ps1           #   PowerShell 版启动脚本
│   ├── requirements.txt    #   依赖清单
│   └── Dockerfile          #   容器化（可选）
├── extension/              # 浏览器扩展
│   ├── manifest.json       #   扩展清单
│   ├── content.js          #   主逻辑 + 样式（样式内嵌在文件顶部的 CSS 字符串里）
│   └── content.css         #   ⚠ 死文件，没有被任何地方引用，别改这个
├── zhihu-real.user.js      # 油猴脚本（备选方案，和扩展二选一）
├── verify_zhida.py         # 直答接口连通性自检脚本
└── 使用说明.md             # 本文件
```

---

## 三、环境要求

- **系统**：Windows（主要开发环境）
- **Python**：3.11 及以上（实测 3.13/3.14 可用）
- **浏览器**：Edge 或 Chrome（都是 Chromium 内核，扩展通用）
- **网络**：能访问 `developer.zhihu.com` 和 `www.zhihu.com`

---

## 四、启动后端

### 方式 1：一键启动（推荐）

1. 进入 `backend` 文件夹
2. **双击 `start.bat`**
3. 如果同目录下有 `secret.txt`，会直接用它，跳过输入提示
4. 否则会提示粘 Access Secret：
   - **粘贴 key 回车** = 用真实 AI（消耗额度）
   - **直接回车** = 用 Mock 假数据（不消耗额度，先跑通用）
5. 启动后会打印一块横幅，**重点看这两行**：

```
  模型     : zhida-thinking-1p5
  key 形状 : 40 位十六进制（正常长相）
```

`key 形状` 不是这行字就说明 key 粘错了（最常见的错因：从文档页复制时把说明文字一起粘进来，变成一百多字符 → 401 → 全部降级成假数据）。

### 方式 2：手动启动

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --port 8000        # 注意：没开 --reload，改完代码要手动重启
```

### 验证后端活着

浏览器打开 `http://localhost:8000/api/health`，例如：

```json
{"ok":true,"secret_loaded":true,"secret_shape":"40 位十六进制（正常长相）",
 "model":"zhida-thinking-1p5","cache_entries":23,
 "features":["stream","model-switch"],"last_error":""}
```

- `secret_loaded:false` → 没读到 key，会走 mock
- **`features` 是版本标记**：改完代码重启后看这里有没有多出新条目，用来确认新版真的跑起来了
- `last_error` → 最近一次直答调用失败的原因，排查 401/429 直接看它

---

## 五、加载浏览器扩展（Edge / Chrome 一样）

1. 打开扩展管理页：Edge 输 `edge://extensions`，Chrome 输 `chrome://extensions`
2. 打开「**开发人员模式**」开关
3. 点「**加载解压缩的扩展**」，选中 `extension` 文件夹
4. 扩展列表里出现「犀利评手 · 知乎评论逻辑分析」

> ⚠️ **改了 `content.js` 后，要两步才生效：**
> 1. 扩展管理页点「**重新加载**」（刷新箭头）
> 2. **回到知乎页面按 F5 刷新**
>
> 只做第 1 步不够 —— content script 只在**页面加载时**注入一次，
> 已经开着的标签页里跑的还是旧脚本。这个坑踩过好几次，症状是「改了没反应」。

---

## 六、怎么用

加载扩展 + 后端跑起来后，打开知乎页面，右下角会出现 **4 个按钮**：

| 按钮 | 在哪种页面 | 做什么 |
|---|---|---|
| ⚔️ **分析评论**（蓝） | 回答页 / 文章页 | 抓该帖下的评论，逐条分析，**卡片边出边显** |
| | **问题页** | 问题全扫：面板里选「前 3 / 5 / 10 个回答」，逐个分析「正文 + 高赞评论」，最后出整体争议 |
| 🧠 **思维导图**（绿） | 任意帖子页 | 把当前正文转成思维导图 |
| ✍️ **写评论**（紫） | 任意页面 | 打开输入框，打字停顿 1.5 秒自动挑逻辑漏洞 |
| 📚 **故事/知识**（橙） | 任意页面 | 黑客松专属内容 |

### 演示页（`/demo`）—— 也是要交的那个公网 Demo

打开 `http://localhost:8000/demo`，是一整个**复刻的知乎单页应用**，不依赖扩展：

| 路由 | 页面 |
|---|---|
| `#/` | 首页（推荐流） |
| `#/hot` | 热榜（20 条，带热度值和排名） |
| `#/discover` | 发现 |
| `#/vip` | 会员 |
| `#/q/q1` … `#/q/q6` | 6 个**有真实内容**的问题详情页 |

- 推荐流和热榜里绝大多数条目是**演示桩**，点开会提示；
  带 `▶ 可点开` 的 **6 个问题**才有正文和评论，能跑完整的 AI 流程。
- 详情页右下角是**一个可展开的蓝色 FAB**，收着 4 个功能：
  分析评论区 / 写评论实时润色 / 正文转思维导图 / 知乎故事知识。
- 搜索框输入关键词回车，能匹配到那 6 个问题（搜「AI」「消费」「转行」「面试」「体制内」）。
- 手机上也做了响应式：窄屏自动收成单列。

> 顶部那条黄色横幅保留着「这是复刻的知乎界面（不是真实知乎）」——
> 既是诚实，也是提交前检查第 7 条要求的「别把演示说成线上已可用」。

### 两个页面类型的行为差异（重要）

- **回答页 / 文章页**：抓到评论后**不立刻显示**，等 AI 分析回来才「评论原文 + 卡片」一起出现。
  谁先分析完谁先冒出来，所以顺序是按完成先后，不是原始楼层顺序。
- **问题页**：面板**立刻弹出**，选完回答数后逐个跑。第一个回答走快模型（首屏快），后面走深度思考模型。

---

## 七、后端接口一览

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/analyze` | POST | 评论逻辑分析（一次性返回整包 JSON） |
| `/api/analyze/stream` | POST | 同上，但用 SSE 一条一条推（前端靠它边出边显） |
| `/api/polish` | POST | 评论润色（挑漏洞 + 改好版本） |
| `/api/mindmap` | POST | 正文转思维导图 |
| `/api/stories` | GET | 黑客松知乎故事列表（免费） |
| `/api/knowledge` | GET | 黑客松知乎知识列表（免费） |
| `/api/story/{id}` | GET | 故事/知识详情（免费） |
| `/api/health` | GET | 健康检查 / 版本标记 / 最近错误 |
| `/demo` | GET | 演示页面 |
| `/docs` | GET | FastAPI 自动生成的接口文档 |

**查询参数：**

| 参数 | 作用 |
|---|---|
| `?force=mock` | 返回假数据，**不烧额度**，调前端样式时用 |
| `?force=live` | 跳过缓存，直连直答 |
| `?model=fast` | 只给 `/api/analyze/stream` 用，换快模型（不思考），首批首屏快几秒 |

**SSE 事件**（`/api/analyze/stream` 会依次推）：

| 事件 | 内容 |
|---|---|
| `meta` | 本次分析几条 + 争论焦点（比逐条分析先出来） |
| `analysis` | 单条卡片：id / stance / gist / logic / flaws |
| `done` | 立场统计 / 漏掉的 id / 相关帖子 / 数据来源 |
| `error` | 出错（流中唯一一个错误事件） |

---

## 八、额度说明（知乎开放平台）

实测本账号各能力的当日额度（用官方 CLI `zhihu-cli quota` 查，随时可查）：

| 能力 | 每日额度 | 本项目用途 |
|---|---|---|
| **知乎直答** | **5000 次** | 评论分析、思维导图、润色 |
| 知乎搜索 | 5000 次 | 相关帖子推荐 |
| 全网搜 | 5000 次 | 未使用 |
| 知乎用户数据 | 10000 次 | 未使用 |
| 热榜 / 问题回答 / 创作能力 | 各 100 次 | 未使用 |
| 知识库 | 500 次 | 未使用 |
| 黑客松故事/知识 | 免费 | 无需鉴权 |

> **重要**：文档里常说的「直答 100 次/天」是**未实名低额度用户**的档位，
> 本账号是 **5000 次/天**，非常充裕，不用省着用。
> 一个回答页 ≈ 3 次调用（分批 5/10/10），一个问答页 ≈ 回答数 + 1 次。
>
> 注意：**Cache 命中不消耗额度**（同样的评论不重复烧）。

---

## 九、常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| 点按钮提示「后端 HTTP 404/连接不上」 | 后端没启动 | 双击 `start.bat` |
| 分析结果 `_source` 是 `mock`，界面顶部有黄/红条 | 没设 secret、key 粘错、或额度用尽 | 看后端黑窗口的 `key 形状` 和 `/api/health` 的 `last_error` |
| 点「分析评论」提示「没抓到评论」 | 页面没评论，或知乎改了评论接口 | 换个有评论的帖子；详见下方「爬评论的坑」 |
| 改了 `content.js` 没反应 | 只重载了扩展，没刷新页面 | 扩展「重新加载」**+ 知乎页面 F5** |
| 弹黄色条「流式没接上，已退回整批分析」 | 后端不是最新版（没有 stream 路由） | 重启后端，看 `/api/health` 的 `features` |
| 知乎评论要登录 | 评论接口需要登录态 | 浏览器保持知乎登录状态 |

### 爬评论的坑（实测记录，别重复踩）

知乎网页端有**两版评论接口**：

- **v4** `/api/v4/{answers|articles}/{id}/comments?order=normal&limit=N&offset=M` —— ✅ **能用的那条**
- **v5** `/api/v4/comment_v5/{...}/{id}/root_comment` —— ❌ **会回空页**：明明 `paging.totals` 说有 N 条，`data` 却是 `[]`，且 `next == previous`、`is_start=false`。大概率要 `x-zse-96` 签名，脚本伪造不了

代码里 `commentUrls()` 做成了「按顺序试，遇到『说有 N 条却回空页』就换下一版」。
**看到 `data:[]` 别直接以为"这页没评论"**，先比对 `counts.total_counts` / `paging.totals`。

---

## 十、安全提醒

- **Access Secret 不要提交到代码仓库、不要截图、不要打进压缩包发给无关人员**
- `backend/secret.txt` 存的是明文 key（方便反复核对，避免往命令行粘错）。**它已被排除在交接包之外**
- 队友要用的话，两条路：① 你在安全渠道单独发给他；② 他用自己的知乎账号在 `developer.zhihu.com` 生成一个
- 后端 `cache.json` 是缓存，可随时删除，会自动重建

---

## 十一、交接给队友：当前状态与待办

### 已完成并验证

- 后端：直答接入（含 `X-Request-Timestamp` 鉴权）、缓存、mock 兜底、启动横幅自检
- **流式分析**（`/api/analyze/stream`，SSE）：逐条抠 JSON 增量解析、边出边显
- 评论抓取：v4 优先 + v5 回退
- 回答页：边抓边分析、卡片按完成顺序冒出来
- 问题页：立刻开面板、面板内选回答数、逐回答流式、整体汇总
- 首批用快模型（`?model=fast`）压首屏时间
- 写评论润色、思维导图、相关帖子、黑客松故事/知识

### 已知问题 / 可以捡的分

按性价比排序，前三个是我想做但没来得及的：

1. **`extension/content.css` 是死文件** —— 没被 `manifest.json` 引用，实际样式全在 `content.js` 顶部的 `CSS` 字符串里。它里面有一套 CSS 变量 + 完整暗色模式，等于白写了。**要么接上（暗色免费生效），要么删掉**——最糟的是留着不改还去改它。
2. **「评论建议」口径不一致** —— `zhida.py` 里有 `result.pop("suggestions", None)`，把「给你 3 条可直接发的评论」这个功能删了；但 `main.py` 的接口文档、产品描述里还写着它。要给评委看 `/docs` 的话，两边对不上。
3. **思维导图不是图** —— `mindmapHtml()` 只是用 `margin-left` 做缩进文字列表，撑不住"思维导图"这名字。至少加连接线。
4. 扩展右下角 4 个悬浮按钮堆积、风格不统一，建议收成一个 FAB。**`demo.html` 里已经这么改了**（单个可展开的 FAB），扩展侧照抄即可。
5. 卡片和「写评论」没打通：看完一条评论的分析想回复，得手动开另一个面板重新打字。**点卡片 → 带上下文预填**是最自然的闭环。
6. 失败的那批没有重试按钮，只能刷新页面重来。
7. 问题页是 N+1 次**串行**调用（答案多时仍慢），可以并行化。
8. 思维导图 / 故事 / 润色里还在用 `alert()` `prompt()` 这类阻塞式弹窗（主路径已清干净）。

### 改代码后必做

1. 后端改完 → **重启**（没开 `--reload`），然后看 `/api/health` 的 `features` 确认版本
2. 前端改完 → 扩展「重新加载」**+ 刷新知乎页面**
3. 调前端样式 → 用 `?force=mock`，不烧额度

---

## 十二、把 Demo 挂到公网（提交要的那个「链接」）

### 先想清楚：评委拿到的是链接，装不了扩展

提交要求是「**可公开访问并能实际操作的线上 Demo**」。评委点开一个网址，
不可能去 `chrome://extensions` 加载你的解压包 —— 所以**公网 Demo 不是插件，是 `/demo` 那一页**。

`backend/demo.html` 已经写好了：一整个复刻的知乎单页应用（首页 / 热榜 / 发现 / 会员 /
6 个能点开的问题详情页），**不依赖扩展**，右下角 FAB 就是 4 个 AI 功能的入口。
扩展仍然照常发，作为「真实场景接入知乎」的加分项（仓库里一起放着）。

> 早期版本这一页的接口写死在 `http://localhost:8000`，换域名必挂；
> 现在改成同源相对路径（`const API = ""`），本机 `localhost:8000/demo`
> 和线上 `https://你的域名/demo` 共用同一份代码，不用改。

### 要动的东西就一样：`ZHIHU_ACCESS_SECRET`

`zhida.py` 读 key 的优先级是 **环境变量 → `secret.txt`**。
线上把 key 放进平台的「环境变量」里就行，**别把 `secret.txt` 传上去**（`.gitignore` 已经挡了）。

### 步骤（下面用 Zeabur 举例，Render / Railway / Koyeb 命令几乎一样，因为 `Dockerfile` 已经能跑）

1. 把 `zhihu-hackathon` 整个文件夹推到一个 GitHub 仓库（私有也行）。
   **推之前先确认 `git status` 里没有 `backend/secret.txt` 和 `backend/cache.json`。**
2. 打开 <https://zeabur.com>，用 GitHub 登录。
3. `New Project` → 区域选**香港 / 新加坡**（国内访问比欧美节点快）。
4. `Add Service` → `Deploy from GitHub` → 选中刚推的仓库。
5. **Root Directory 填 `backend`**（Dockerfile 在这个子目录里），构建方式它自己会认 Dockerfile。
6. 进 `Variables`，加一条：`ZHIHU_ACCESS_SECRET` = 你的 key。
7. `Networking` → 生成域名，端口填 `8000`，会拿到一个 `https://xxx.zeabur.app`。
8. 浏览器打开 **`https://xxx.zeabur.app/demo`** —— 这就是要提交的链接。
9. 顺手验一遍：`https://xxx.zeabur.app/api/health` 里 `secret_loaded` 得是 `true`，
   `secret_shape` 要是「40 位十六进制（正常长相）」。

### 提交前的自检（对着活动页的检查清单过）

| 检查项 | 怎么验 |
|---|---|
| Demo 公网可打开、核心流程能跑通 | 手机流量（不开 WiFi）打开 `/demo`，点按钮，卡片逐条冒出来 |
| 凭证没出现在仓库/前端/日志/截图/视频 | 仓库里搜 `secret`；`/api/health` 只吐形状不吐内容；录屏别录 `start.bat` 的黑窗口 |
| 失败/额度耗尽/空数据有真实提示 | 线上临时把 key 删掉再点一次，确认弹的是降级提示而不是白屏 |
| 代码仓库、视频链接评委能打开 | 用无痕窗口打开一次（私有仓库评委看不到） |

### 两个只有线上才会踩的坑

- **平台会接管端口**：`Dockerfile` 里是 `uvicorn main:app --port ${PORT}`，
  平台自动注入 `PORT`，本地那个 `8000` 只是默认值，不用改。
  如果平台不让用 Dockerfile，启动命令就填 `uvicorn main:app --host 0.0.0.0 --port $PORT`。
- **免费实例会休眠**：Render 免费层 15 分钟没人访问就睡，醒来要 50 秒以上，
  评委点开正好撞上冷启动 → 以为你的作品是坏的。
  **提交完自己先点一次唤醒**，或者干脆升一档付费实例撑过评审期。

### 公开之后记得防额度滥用

链接一公开，任何人点按钮都在烧你账号的直答额度（5000 次/天）。
评审期够用，但别把链接发到大群里。真要发，在 `main.py` 里加个按 IP 的简单限流，
或者把 `?force=mock` 作为默认（假数据演示，不烧额度，但指标里 `_source` 会是 `mock`，
检查项第 7 条不建议长期这么干）。
