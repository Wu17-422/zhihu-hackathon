# 犀利评手 · 知乎评论区争议地图

> 给知乎评论区装一张「地图」：800 条评论压成几个阵营，一眼看清大家在吵什么、
> 分歧的根在哪、以及哪条好评论被排序埋掉了。

---

## 一、项目是什么

一个浏览器扩展（Edge / Chrome 通用）+ 本地后端。你在逛知乎时点一下按钮，AI 帮你：

| 功能 | 说明 |
|---|---|
| 🗺️ **争议地图**（首屏） | N 条评论 → 3~5 个阵营 + 各自论据 + 代表原文，渲染成 SVG 导图 |
| 🎯 **分歧根源** | 双方其实卡在哪个具体判断上（不是「一方认为A、一方认为B」这种废话） |
| 💎 **被埋没的好评论** | 赞数低、排在后面，但质量明显高于平均的那几条 |
| 🔍 **逐条逻辑分析** | 折叠区里，点开才看：立场 + 想表达什么 + 推理链 + 逻辑漏洞 |
| 🧠 **思维导图** | 把帖子/回答正文转成 SVG 树状导图 |
| ✍️ **写评论实时检测** | 边打字边概括观点 + 挑逻辑漏洞 + 并行搜相关讨论 |
| 📚 **黑客松故事/知识** | 知乎黑客松专属内容接口（免费） |

**为什么首屏是争议地图而不是逐条卡片**：输入 800 条评论、输出 800 张卡片，
比原文还长，压缩工具就失去意义了。逐条分析保留，但降级成点开才看的细节。

---

## 二、目录结构

仓库是平铺的：后端全在 `backend/`，扩展全在 `extension/`。
开发时按板块分工，落库时合成这一份。

```
zhihu-hackathon/
├── backend/                     # 后端（Python / FastAPI）
│   ├── main.py                  #   入口：路由 + 启动横幅 + /api/health
│   ├── zhida.py                 #   直答封装 + 争议聚合 + 坏 JSON 修复 + 缓存 + 流式
│   ├── prompt.py                #   提示词（产品灵魂）
│   ├── mock.py                  #   假数据兜底
│   ├── demo.html                #   ★ 提交用的那个 Demo（复刻版知乎单页应用）
│   ├── Dockerfile               #   线上部署用（Render / Railway 等都吃它）
│   ├── requirements.txt
│   ├── start.bat / start.ps1    #   一键启动
│   ├── secret.txt               #   🔴 明文 key，见第十节（.gitignore 已挡）
│   └── cache.json               #   缓存（可删，会自动重建）
├── extension/                   # 浏览器扩展（Edge / Chrome 通用）
│   ├── manifest.json            #   加载顺序：connector → contentA → contentB
│   ├── connector.js             #   共享接口 window.__zha
│   ├── contentA.js              #   板块A：争议地图 + 逐条分析 + 思维导图（CSS 内嵌在文件顶部）
│   └── contentB.js              #   板块B：写评论润色 + 故事/知识（CSS 内嵌）
├── zhihu-real.user.js           # 油猴脚本（备选方案，和扩展二选一；未跟进本次改造）
├── verify_zhida.py              # 直答接口连通性自检
└── 使用说明.md                  # 本文件
```

> ⚠️ **扩展的入口是 `extension/` 这个文件夹**，`manifest.json` 在里面。
> 加载解压缩的扩展时要选它，不是仓库根目录。

---

## 三、环境要求

- **系统**：Windows（主要开发环境）
- **Python**：3.11 及以上（实测 3.13 / 3.14 可用）
- **浏览器**：Edge 或 Chrome（都是 Chromium 内核，扩展通用）
- **网络**：能访问 `developer.zhihu.com` 和 `www.zhihu.com`
- **知乎登录态**：评论接口需要登录，浏览器保持登录知乎

---

## 四、启动后端

### 方式 1：一键启动（推荐）

1. 进入 `backend` 文件夹
2. **双击 `start.bat`**
3. 如果同目录有 `secret.txt`，会直接用它，跳过输入提示
4. 否则会提示粘 Access Secret：
   - **粘贴 key 回车** = 用真实 AI（消耗额度）
   - **直接回车** = 用 Mock 假数据（不消耗额度）
5. 启动后会打印一块横幅，**重点看这两行**：

```
  模型     : zhida-thinking-1p5
  key 形状 : 40 位十六进制（正常长相）
```

`key 形状` 不是这行字就说明 key 粘错了（最常见错因：从文档页复制时把说明文字一起粘进来，
变成一百多字符 → 401 → 全部降级成假数据）。

### 方式 2：手动启动

```bash
# 注意：在 backend 目录里执行
pip install -r requirements.txt
py -m uvicorn main:app --port 8000
```

### 验证后端活着

浏览器打开 `http://localhost:8000/api/health`：

```json
{"ok":true,"secret_loaded":true,"secret_shape":"40 位十六进制（正常长相）",
 "model":"zhida-thinking-1p5","cache_entries":23,
 "features":["stream","model-switch","consensus","buried-gems","related"],"last_error":""}
```

- `secret_loaded:false` → 没读到 key，会走 mock
- **`features` 是版本标记**：改完代码重启后看这里。有 `consensus` 才是带争议地图的新版
- `last_error` → 最近一次直答调用失败的原因，排查 401/429 直接看它

---

## 五、加载浏览器扩展（Edge / Chrome 一样）

1. 打开扩展管理页：Edge 输 `edge://extensions`，Chrome 输 `chrome://extensions`
2. 打开「**开发人员模式**」开关
3. 点「**加载解压缩的扩展**」，**选中 `extension` 文件夹**（manifest.json 在那里）
4. 扩展列表里出现「犀利评手」

> ⚠️ **改了前端后要三步才生效：**
> 1. `cp` 同步到 `extension/`（见第二节）
> 2. 扩展管理页点「**重新加载**」（刷新箭头）
> 3. **回到知乎页面按 F5**
>
> 只做前两步不够 —— content script 只在**页面加载时**注入一次，
> 已经开着的标签页跑的还是旧脚本。症状是「改了没反应」。

---

## 六、怎么用

右下角一个 ⚡ 悬浮球，点开是菜单：

| 菜单项 | 在哪种页面 | 做什么 |
|---|---|---|
| 🗺️ **争议地图**（蓝） | 回答页 / 文章页 | 读该帖评论 → 阵营 + 分歧根源 + 被埋没的好评论 |
| | **问题页** | 扫前 10 个回答的评论区，合起来做一张地图 |
| 🧠 **思维导图**（绿） | 任意帖子页 | 把当前正文转成 SVG 导图 |
| ✍️ **写评论**（紫） | 任意页面 | 打字停顿 1.5 秒自动挑逻辑漏洞 |
| 📚 **故事/知识**（橙） | 任意页面 | 黑客松专属内容 |

**演示页**：`http://localhost:8000/demo` —— 一个照着真知乎搭的整站复刻（首页 / 热榜 /
发现 / 会员 / 6 个能点开的问题详情页），**不依赖扩展**，右下角悬浮球就是 AI 功能的入口。

> 这一页是**提交要求的那个「公网 Demo」本体**：评委拿到的是链接、装不了扩展，
> 所以 `/demo` 必须自己就能演完全部功能。详见第十三节。
>
> 它的接口写的是同源相对路径（`const API = ""`），所以本机 `localhost:8000/demo`
> 和线上 `https://你的域名/demo` 共用同一份代码，换域名不用改任何东西。

### 争议地图的流程

1. **读评论**：面板实时显示「已读 N 条」。回答页最多读 200 条；问题页扫前 10 个回答、
   每答最多 40 条，总上限 600 条
2. **归并阵营**：抽样最多 60 条送给模型（**抽样一半留给低赞和靠后楼层**，
   否则「被埋没的好评论」永远选不出来），压成 3~5 个阵营
3. **渲染**：阵营比例条 → SVG 争议地图 → 分歧根源 → 被埋没的好评论 → 各阵营详情
4. **想看细节**：底部「🔍 展开逐条分析」，点开才跑流式逐条分析，卡片贴到每条评论下面

条形图第 i 段的颜色和导图第 i 个分支是同一个色，两处对照着看。

---

## 七、后端接口一览

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/consensus` | POST | **争议地图**：阵营 + 分歧根源 + 被埋没的好评论 + 导图树 |
| `/api/analyze` | POST | 逐条逻辑分析（一次性返回整包 JSON） |
| `/api/analyze/stream` | POST | 同上，SSE 一条一条推 |
| `/api/related` | POST | 草稿 → 5 条相关讨论（**只搜不调模型，约 1 秒**） |
| `/api/polish` | POST | 草稿 → 概括观点 + 挑逻辑漏洞（**不再返回改写版本**） |
| `/api/mindmap` | POST | 正文转思维导图 |
| `/api/stories` | GET | 黑客松故事列表（免费） |
| `/api/knowledge` | GET | 黑客松知识列表（免费） |
| `/api/story/{id}` | GET | 故事/知识详情（免费） |
| `/api/health` | GET | 健康检查 / 版本标记 / 最近错误 |
| `/demo` | GET | 演示页面 |
| `/docs` | GET | FastAPI 自动生成的接口文档 |

**查询参数：**

| 参数 | 作用 |
|---|---|
| `?force=mock` | 返回假数据，**不烧额度** |
| `?force=live` | 跳过缓存，直连直答 |
| `?model=fast` | 只给 `/api/analyze/stream` 用，换快模型 |

> **前端怎么切 mock**：在页面控制台执行 `localStorage.zhaMock = 1`（扩展和 `/demo` 都认这个开关），
> 之后所有前端请求都带 `?force=mock`。恢复真实调用：`delete localStorage.zhaMock`。
>
> 🔴 以前前端把 `?force=mock` **硬编码**在 URL 里（`connector.js`、`contentA.js`、
> `contentB.js` 三处），导致无论 key 对不对，全线跑的都是假数据、真答接口从没被调用过。
> 已改成默认真实调用。界面右上角那个来源角标（🔼 直答 / 📥 缓存 / 🚧 假数据）是准的，
> 演示前先确认它不是「🚧 假数据」。

**`/api/consensus` 返回结构：**

```jsonc
{
  "core_dispute": "真正在争什么，一句话",
  "crux": "双方卡在哪个判断上，两三句",
  "camps": [{
    "name": "阵营名", "claim": "核心主张", "stance": "agree|oppose|neutral",
    "grounds": ["论据1", "论据2"], "size": 12, "ratio": 0.34,
    "comment_ids": ["..."], "quote": {"id":"...","text":"代表原文","author":"...","likes":42}
  }],
  "buried_gems": [{"id":"...","text":"...","author":"...","likes":2,"floor":340,"why":"为什么值得看"}],
  "mindmap": {"name":"根","children":[]},   // 直接喂给前端 mindmapHtml()
  "related_posts": [],
  "_analyzed": 60,    // 实际送给模型几条
  "_total": 812,      // 一共读到几条
  "_source": "live|cache|mock"
}
```

> ⚠️ 字段别改名成 `_sampled` 之类以 `_sa` 开头的名字。FastAPI 的 `jsonable_encoder`
> 默认 `sqlalchemy_safe=True`，会**静默丢掉**所有 `_sa*` 键（它以为是 SQLAlchemy 内部字段）。
> 直接调函数看得到，过了 HTTP 就没了，排查起来很费时间。

### 🔴 大提示词必须走流式，别改回非流式

`zhida.py` 里有两个调用入口，用错会挂：

| 函数 | 用法 |
|---|---|
| `zhida_chat()` | `stream=False`。**只适合一句话的短请求**（比如 `--ping`） |
| `zhida_chat_collect()` | `stream=True`，收齐再返回。**所有大提示词都必须走它** |
| `_chat_json()` | 在上面那个之外再包一层：坏 JSON 自动修 + 不可用时重试一次 |

争议聚合、思维导图、逐条分析（整批兜底）三个入口现在都走 `_chat_json()`。

**为什么**：thinking 模型遇到大提示词会先「想」很久才吐第一个字，非流式请求在这段
空等里连一个字节都没有，直答的服务端会把它当死连接掐掉：

```
ConnectionResetError(10054) 远程主机强迫关闭了一个现有的连接
```

2026-09-15 实测：争议聚合（提示词约 4KB、15 条评论）用非流式**必挂**，跑几分钟才断；
换成流式后同一个提示词稳定在 25~45 秒返回。逐条分析（`stream_analyze`）当初大概也是
踩了同一个坑才写成流式的。

### 坏 JSON 的两层兜底

模型**偶发**吐出不合法的 JSON。实测遇到过 `Expecting ',' delimiter: line 36 column 6`，
同一个提示词重跑又是好的 —— 偶发比稳定失败更危险，因为演示时会随机降级成假数据，
而右上角那个角标很小，台上没人会注意。

所以有两层：

1. `_repair_json()` 修三类常见毛病 —— 字符串里没转义的裸双引号（最常见的死因）、
   尾随逗号、生成被截断（补齐没闭合的引号和括号）。有单测覆盖 9 种情况。
2. 修不动就**重打一次**（`_chat_json` 的 `tries=2`）。只有「JSON 不可用」会重试；
   401/403/429 直接往上抛，不浪费额度。

**SSE 事件**（`/api/analyze/stream`）：

| 事件 | 内容 |
|---|---|
| `meta` | 本次分析几条 + 争论焦点 |
| `analysis` | 单条卡片：id / stance / gist / logic / flaws |
| `done` | 立场统计 / 漏掉的 id / 相关帖子 / 数据来源 |
| `error` | 出错（流中唯一一个错误事件） |

---

## 八、额度说明（知乎开放平台）

用官方 CLI `zhihu-cli quota` 随时可查。

| 能力 | 每日额度 | 本项目用途 |
|---|---|---|
| **知乎直答** | **5000 次** | 争议聚合、逐条分析、思维导图、润色 |
| 知乎搜索 | 5000 次 | 相关帖子推荐 |
| 全网搜 | 5000 次 | 未使用 |
| 知乎用户数据 | 10000 次 | 未使用 |
| 热榜 / 问题回答 / 创作能力 | 各 100 次 | 未使用 |
| 知识库 | 500 次 | 未使用 |
| 黑客松故事/知识 | 免费 | 无需鉴权 |

> 文档里常说的「直答 100 次/天」是**未实名低额度用户**的档位，本账号是 **5000 次/天**。
>
> **一次争议地图 = 1 次直答调用**（+1 次搜索，用于相关帖子），
> 不管读了 800 条还是 20 条 —— 抽样后只发一个请求。逐条分析才是按批烧（每批 1 次）。
>
> 例外：模型吐了坏 JSON 会自动重打一次，那次就是 2 次。不常见。
>
> **Cache 命中不消耗额度。** 评论内容没变就直接返回缓存。
>
> 实测耗时（15 条评论、thinking 模型）：争议地图 25~45 秒，思维导图约 4 秒，
> 缓存命中 0.01 秒。演示时第一次跑要有心理准备，讲解正好填这段等待。

> ℹ️ 评论读取走的是**知乎网页端接口 + 浏览器登录态**，不是开放平台能力，
> 所以不占上面任何额度，但也意味着知乎改接口就会断（见第九节）。

---

## 九、常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| 面板显示「后端没响应」 | 后端没启动 | 双击 `start.bat`，看 `/api/health` |
| 右上角角标是「🚧 假数据」 | 没设 secret、key 粘错、或额度用尽 | 看后端黑窗口的 `key 形状` 和 `/api/health` 的 `last_error` |
| 「读评论失败」 | 没登录知乎，或知乎改了评论接口 | 确认浏览器登录状态；详见下方「爬评论的坑」 |
| 「没读到评论」 | 这条内容真的没评论 | 换个有评论的帖子 |
| 改了前端没反应 | 没同步到 `extension/`，或没刷新页面 | `cp` 同步 → 扩展「重新加载」→ 知乎页面 F5 |
| 后端起不来，报 `ImportError` | 在 `backend` 里直接跑了 `main.py` | 在 **`backend` 目录**里跑 `py -m uvicorn main:app --port 8000` |
| `features` 里没有 `consensus` | 后端是旧版 | 重启后端（`start.bat` 带 `--reload`，但导入结构改动要手动重启） |
| 后端窗口报 `ConnectionResetError(10054)` | 某处把大提示词改回了非流式 `zhida_chat()` | 改用 `_chat_json()`，见第七节「大提示词必须走流式」 |
| 面板上写「读了 undefined 条评论」 | 缓存命中时漏补下划线字段 | 已修；缓存里不存 `_` 开头的键，命中时要手动补回 `_analyzed` / `_total` |

### 爬评论的坑（实测记录，别重复踩）

知乎网页端有**两版评论接口**：

- **v4** `/api/v4/{answers|articles}/{id}/comments?order=normal&limit=N&offset=M` —— ✅ **能用的那条**
- **v5** `/api/v4/comment_v5/{...}/{id}/root_comment` —— ❌ **会回空页**：明明 `paging.totals`
  说有 N 条，`data` 却是 `[]`，且 `next == previous`、`is_start=false`。
  大概率要 `x-zse-96` 签名，扩展里伪造不了。**别再花时间试它。**

> 🔴 本文档以前写着代码里做了「v4 优先 + v5 回退」—— **那是不实的，回退逻辑从来不存在**，
> `commentUrls()` 返回的数组只有 v4 一个元素。现在代码里也只有 v4 一条路，
> 注释里写清了为什么不补 v5。

**二级评论**：v5 走不通，所以「谁回复谁」只能从 v4 一级评论内嵌的 `child_comments`
里顺手捡，捡到多少算多少，不保证完整。

另外三个曾经在悄悄坏事的坑（都已修）：

- 评论赞数字段是 **`vote_count`**，不是 `like_count`。原来只读后者，
  所有评论的赞数永远是 0 —— 按赞排序、抽样、被埋没的好评论全部失效
- `c.author` 是**对象**，不是字符串。原来直接渲染，界面上显示 `[object Object]`
- `c.content` 是 **HTML**（`<p>`、表情 `<img>`、@人的 `<a>`），要先剥标签再送给模型

---

## 十、安全提醒

✅ **key 已经挪到交接包外面了**，现在在 `Desktop\1\secret.txt`（和交接包同级的上一层）。
包内不再有任何 `secret.txt`，可以直接分享。

（本文档以前写着「已被排除在交接包之外」，但当时它其实还在 `backend/` 里躺着 —— 那句是不实的。现在是真的。）

**key 的三条查找路径**（`zhida.py` 的 `_load_secret()`，`start.bat` / `start.ps1` 口径一致）：

1. 环境变量 `ZHIHU_ACCESS_SECRET`（key 本身）
2. 环境变量 `ZHIHU_SECRET_FILE`（指向一个存 key 的文件，**可以在项目外面**）
3. 包内 `backend/secret.txt`（现在没有，也别再放回去）

启动脚本会**自动往上找几层目录**里的 `secret.txt`，找到就用环境变量指过去，
不把它复制进项目。所以你双击 `start.bat` 什么都不用做，横幅上会打印：

```
Using secret file outside the package:
  C:\Users\34523\Desktop\1\secret.txt
```

- 换机器/换人时：把 `secret.txt` 放在项目文件夹的上一层就行
- 队友要用，两条路：① 你在安全渠道单独发；② 他用自己账号在 `developer.zhihu.com` 生成
- **别为了图方便把 key 复制回 `backend/`** —— 那是它最容易跟着压缩包一起发出去的地方
- `cache.json` 里有缓存的**他人评论原文**。如果这个项目要对外发布，
  这点需要明确一下口径（只缓存分析结果、或加开关），现在是全存
- **Access Secret 不要提交到仓库、不要截图、不要打进压缩包**

---

## 十一、交接给队友：当前状态与待办

### 已完成并验证

- **争议地图**（`/api/consensus`）：阵营归并 + 分歧根源 + 被埋没的好评论，
  复用已有 SVG 导图引擎渲染，一次调用搞定
- 抽样策略：一半配额留给低赞和靠后楼层（实测 200 条抽 60 条，
  样本里有 31 条 0 赞、16 条百楼之后）
- 脏数据防御：编造的编号、跨阵营重复归类、`quote_id` 不在组内、
  拿高赞冒充「被埋没」—— 全部挡掉，有单测覆盖
- 后端启动修复：`zhida.py` / `mock.py` 的导入在平铺目录下不再 `ImportError`
- 前端默认走真实调用（三处 `?force=mock` 硬编码已清）
- 评论读取修复：`vote_count`、作者对象、HTML 剥标签、翻页上限可配
- 立场标签配色修复（类名和 CSS 对不上，以前一直是无色的）
- 阻塞式 `alert()` / `prompt()` 全部换成面板内提示
- 失败有重试按钮，不用刷新整页
- 逐条流式分析、写评论润色、思维导图、相关帖子、黑客松故事/知识

### 已知问题 / 可以捡的分

按性价比排序：

1. **卡片和「写评论」没打通** —— 看完一条评论的分析想回复，还得手动开另一个面板重新打字。
   **点卡片 → 带上下文预填**是最自然的闭环，也是跨 A/B 板块的活
2. **问题页是串行读取** —— 前 10 个回答一个个读，答案多时慢。可以并行，
   但要注意知乎限流（现在每个请求间隔 120ms）
3. **阵营划分没法人工纠正** —— 模型偶尔会把两个阵营切得太细或并得太粗，
   用户看出来了也改不动。允许拖动合并会很亮眼，但工作量不小
4. **二级评论不完整** —— 只能捡 `child_comments` 里内嵌的那几条，
   完整的回复树需要 `x-zse-96` 签名，第三方做不到
5. **`zhihu-real.user.js`（油猴脚本）没跟上这次改造** —— 它还是旧的逐条分析版本，
   也还带着 `force=mock` 硬编码。要么同步，要么在文档里标明它已过时
6. **暗色模式没做** —— 现在的 CSS 是写死的浅色值，知乎暗色模式下面板会很刺眼

### 改代码后必做

1. 前端改完（`extension/contentA.js` / `contentB.js`）→ 扩展页「重新加载」→ 知乎页面 F5
2. 后端改完 → 重启 → 看 `/api/health` 的 `features` 确认版本
3. 调前端样式 → 控制台 `localStorage.zhaMock = 1`，不烧额度
4. 演示前 → 确认右上角角标是「🔼 直答」而不是「🚧 假数据」

---

## 十二、调提示词：`verify_zhida.py`

提示词决定 90% 的效果，而它只能用真数据验。这个脚本就是干这个的 ——
**不启动后端、不加载扩展**，直接打直答看结果。

```bash
py verify_zhida.py --ping      # 只测连通性，最省额度
py verify_zhida.py --camps     # 验争议地图 ← 首屏，最该盯这个
py verify_zhida.py             # 验逐条分析（现在只是折叠区里的细节）
```

`--camps` 和默认模式**各消耗 1 次直答额度**。攒够几处改动再跑，别改一句跑一次。

改提示词只改 `backend/prompt.py`（脚本和线上读的是同一份），这个脚本本身不用动。

> 这个脚本以前是**跑不起来**的：`sys.path` 插的是 `backend/`，但后端在 `backend/`，
> 一跑就 `ModuleNotFoundError: No module named 'prompt'`（拆板块时漏改了）。
> 它还只认环境变量里的 key，key 挪到包外之后就再也读不到。两处都修了，
> 现在取 key 的三条路径和后端完全一致。
>
> 另外它原来会打印一段「评论建议」（`suggestions`）——
> 那个功能早就在 `zhida.py` 里删掉了（`result.pop("suggestions", None)`），
> 所以永远只输出一个空标题。这正是旧文档里列的已知问题 #2，已清掉。

**跑 `--camps` 时重点看这五件事**（脚本跑完会自己打印一遍）：

1. **阵营是不是真的互相区分？** 有没有两个阵营其实是一个意思换句话说 —— 这是产品核心，切错了首屏就是错的
2. **「被埋没的好评论」挑得准吗？** 内置测试数据里，`id 11`（code review 成本数据）
   和 `id 12`（并发是硬伤）是该被捞出来的；如果挑的是 `id 13`「楼上说得对」
   这种一句话附议，说明提示词要再强调「有内容」
3. **分歧的根（`crux`）说透了没？** 「一方认为A，一方认为B」是废话，要指出两边用的标准差在哪
4. 阵营名是不是 12 字以内、一眼看出立场
5. 覆盖率 —— 大量评论没归进任何阵营，要么阵营太少，要么提示词没说清

> ⚠️ **mock 的「被埋没的好评论」也有同样的陷阱。** 它原来取「赞数最低的三条」，
> 在演示页上挑出来的第一条就是「楼上说得对，工具就是工具。」——
> 台上真答一挂降级到 mock，招牌功能会当场展示三条废话。
> 现在改成「先取赞数靠后的一半，再在里面挑最有内容的」（用文本长度做粗代理）。

---

## 十三、把 Demo 挂到公网（提交要的那个「链接」）

### 先想清楚：评委拿到的是链接，装不了扩展

提交要求是「**可公开访问并能实际操作的线上 Demo**」。评委点开一个网址，
不可能去 `chrome://extensions` 加载你的解压包 —— 所以**公网 Demo 不是插件，是 `/demo` 那一页**。

`backend/demo.html` 已经写好了：一整个复刻的知乎单页应用（首页 / 热榜 / 发现 / 会员 /
6 个能点开的问题详情页），**不依赖扩展**，右下角 FAB 就是 4 个 AI 功能的入口。
扩展仍然照常发，作为「真实场景接入知乎」的加分项（仓库里一起放着）。

> 早期版本这一页的接口写死在 `http://localhost:8000`，换域名必挂；
> 现在改成同源相对路径（`const API = ""`），本机 `localhost:8000/demo`
> 和线上 `https://你的域名/demo` 共用同一份代码，不用改。

### 要动的东西就一样：`ZHIHU_ACCESS_SECRET`

`zhida.py` 读 key 的优先级是 **环境变量 → `secret.txt`**。
线上把 key 放进平台的「环境变量」里就行，**别把 `secret.txt` 传上去**（`.gitignore` 已经挡了）。

### 实际用的平台：腾讯云开发云托管

线上跑的是 <https://backend-314091-9-1472846425.sh.run.tcloudbase.com>
（选它是因为**不用绑信用卡**，Render 免费版要 Visa/Mastercard，国内卡是银联收不了）。

`.dockerignore` + 根目录 `Dockerfile` 已经配好，换任何 Docker 平台都能直接构建。

1. 把 `zhihu-hackathon` 整个文件夹推到 GitHub 公开仓库。
   **推之前先确认 `git status` 里没有 `backend/secret.txt` 和 `backend/cache.json`。**
2. 云开发控制台 → 云托管 → **新建服务** → 上传方式选「代码库拉取」→ 填仓库地址、分支 `main`。
   公开仓库**不用授权**。
3. 环境变量加一条：`ZHIHU_ACCESS_SECRET` = 你的 key。
4. 点「开始部署」，等状态从「创建中」变成「正常」，日志里能看到 `pip install` 在滚。
5. 拿到的域名形如 `https://<服务名>-<数字>.sh.run.tcloudbase.com`，
   浏览器打开 **`.../demo`** —— 这就是要提交的链接。
6. 顺手验一遍：`.../api/health` 里 `secret_loaded` 得是 `true`，
   `secret_shape` 要是「40 位十六进制（正常长相）」。

### 部署踩过的坑

**坑一：Dockerfile 放在 `backend/` 里 —— 已修复，但换平台别再犯**

现象是构建直接失败：

```
ERROR: failed to build: failed to solve: failed to read dockerfile:
open Dockerfile: no such file or directory
```

各平台的**构建上下文都是仓库根目录**（日志里是 `Cloning into '.'`），
只在子目录放 Dockerfile 不认。已挪到仓库根目录，`.dockerignore` 也跟着挪了。

**坑二：域名一直返回 `INVALID_PATH`**

`INVALID_PATH` 的含义是「环境认得，但这个域名下没有转发规则」——
**不是容器没起来，是域名没绑到服务上**。如果又遇到，按顺序查这三项：

1. **有没有真的部署成功一个版本**。服务建好 ≠ 版本建好，是两步。
   在服务详情页点「新建版本」，配置完点「开始部署」，等状态从「创建中」变「正常」。
2. **版本流量是不是 100%**。新建版本时若选了「保持流量为 0」，版本是「正常」
   但不给流量。到「版本管理 → 流量配置」切到 100%。
3. **监听端口是不是和容器一致**。这一栏默认 `80`，容器听的是 `8000`，
   不一致就探活失败。填 `8000`。

另外：云托管控制台偶尔会报 `tcb/DescribeEnvBacklogs` → `NETWORK_ERROR`，
那是浏览器侧的问题（接口本身是通的）。**Ctrl+Shift+R 强刷**，或用无痕窗口重开。

### 提交前的自检（对着活动页的检查清单过）

| 检查项 | 怎么验 |
|---|---|
| Demo 公网可打开、核心流程能跑通 | 手机流量（不开 WiFi）打开 `/demo`，点按钮，卡片逐条冒出来 |
| 凭证没出现在仓库/前端/日志/截图/视频 | 仓库里搜 `secret`；`/api/health` 只吐形状不吐内容；录屏别录 `start.bat` 的黑窗口 |
| 失败/额度耗尽/空数据有真实提示 | 线上临时把 key 删掉再点一次，确认弹的是降级提示而不是白屏 |
| 代码仓库、视频链接评委能打开 | 用无痕窗口打开一次（私有仓库评委看不到） |

### 两个只有线上才会踩的坑

- **平台会接管端口**：`Dockerfile` 里是 `uvicorn main:app --port ${PORT}`，
  `ENV PORT=8000` 只是本地默认值，平台注入同名变量就会覆盖它，**Dockerfile 不用改**。
  但平台控制台上那个「监听端口」输入框要填 `8000`（详见上面坑二第 3 条）。
  如果平台不让用 Dockerfile，启动命令就填 `uvicorn main:app --host 0.0.0.0 --port $PORT`。
- **免费实例会休眠**：Render 免费层 15 分钟没人访问就睡，醒来要 50 秒以上，
  评委点开正好撞上冷启动 → 以为你的作品是坏的。
  **提交完自己先点一次唤醒**，或者干脆升一档付费实例撑过评审期。

### 公开之后记得防额度滥用

链接一公开，任何人点按钮都在烧你账号的直答额度（5000 次/天）。
评审期够用，但别把链接发到大群里。真要发，在 `main.py` 里加个按 IP 的简单限流，
或者把 `?force=mock` 作为默认（假数据演示，不烧额度，但指标里 `_source` 会是 `mock`，
检查项第 7 条不建议长期这么干）。
